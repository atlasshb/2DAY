// Placeholder shell — the Fieldkit layout (docs/07-ui-concepts.md) lands here.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
